# تحديث: الإصلاحات + الاقتراحات (١٣ ملفًا متغيّرًا)

ارفع هذه الملفات إلى GitHub بمساراتها كما هي (Add files via upload). **لا حاجة لرفع بقية المشروع.**
سير النشر يستخدم `npm install`. البناء `vite build` يمرّ بلا أخطاء.

## أ) إصلاحات قواعد الأمان — `firestore.rules`
- إنفاذ **قفل الفترة**: لا كتابة/تعديل في شهر مقفل إلا بصلاحية `edit_locked_period` (مع فرع دقيق يُبقي «إعادة فتح الفترة» تعمل لصاحب `lock_period`).
- **حماية المالك**: لا يُعدَّل/يُحذف حساب المالك إلا من المالك نفسه.
- **التحقّق من صحّة الحركة** (`validTxn`): تاريخ نصّي (≥7) ومدين/دائن أرقام غير سالبة.
- **الإعدادات**: من يملك `add_transaction`/`lock_period` يكتب حقل `sanadCounter` فقط (لا كامل الإعدادات).
- **صلاحية الاعتماد** وحدها لا تعدّل سوى حالة الاعتماد.
- (بند «المشاهد المحدود» متروك بطلبك — الدور غير مستخدم.)

## ب) تعارض أسماء الحقول (كسر صامت)
- الشعار موحّد على `companyLogo` — يظهر الآن في **كشف QR العام** والطباعة. (`Settings.jsx`, `store.js`, `share.js`)
- حدّ الاعتماد موحّد على `approvalThreshold` — يُطبَّق فعليًّا في نموذج الحركة. (`Settings.jsx`)
- (حقل «أقصى محاولات دخول» أُزيل لأنه لا يعمل: القفل قبل تسجيل الدخول حيث لا تُقرأ الإعدادات.)

## ج/د) الاقتراحات المنفّذة
1. **زر الرجوع** في الشريط العلوي. (`AppShell.jsx`)
2. **الوسوم** تُطبَّق على الموظف والحركة (اختيار من وسوم الإعدادات + عرض على بطاقة الموظف). (`Employees.jsx`, `TransactionModal.jsx`, `AddTransaction.jsx`, `db.js`, `store.js`)
3. **زر QR مباشر** في قائمة الموظفين. (`Employees.jsx`)
4. **عنوان الشركة** في الإعدادات + ترويسة الطباعة + كشف QR. (`Settings.jsx`, `store.js`, `PrintDocs.jsx`, `share.js`, `PublicStatement.jsx`)
5. **أدنى طول لكلمة المرور** قابل للضبط من الإعدادات. (`Settings.jsx`, `ForceSetPassword.jsx`, `Account.jsx`)

> ملاحظة: تعذّر تشغيل محاكي Firebase في بيئة العمل (الشبكة محجوبة)، فالتحقّق من القواعد كان بالمنطق والصياغة + نجاح البناء. يُنصح باختبار سريع بعد النشر: إضافة حركة عادية، محاولة الكتابة في شهر مقفل، ومشاركة رابط QR والتأكد من ظهور الشعار.
