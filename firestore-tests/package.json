{
  "name": "daybook-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "اختبارات وحدات لقواعد أمان Firestore — نظام تقارير اليومية (daybook-dhtrd)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project daybook-dhtrd \"node --test rules.test.mjs\"",
    "test:local": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.2",
    "firebase-tools": "^13.23.1"
  }
}
